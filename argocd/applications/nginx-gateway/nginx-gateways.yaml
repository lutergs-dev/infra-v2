apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-gateway-settings
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: default
  source:
    repoURL: https://github.com/lutergs-dev/infra-v2.git
    targetRevision: main
    path: argocd/manifests/nginx-gateway-settings
  destination:
    server: https://kubernetes.default.svc
    namespace: nginx-gateway
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
