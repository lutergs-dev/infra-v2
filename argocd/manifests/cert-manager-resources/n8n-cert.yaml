apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lutergs-n8n-https-cert
  namespace: nginx-gateway
spec:
  secretName: lutergs-n8n-https-tls
  dnsNames:
    - n8n.lutergs.dev
  issuerRef:
    name: cloudflare-issuer
    kind: ClusterIssuer
    group: cert-manager.io
