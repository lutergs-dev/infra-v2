apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "ghost.fullname" . }}-http-route
spec:
  parentRefs:
    - name: gateway
      namespace: nginx-gateway
      sectionName: blog.lutergs.dev
  hostnames:
    - blog.lutergs.dev
  rules:
    - backendRefs:
        - name: {{ include "ghost.fullname" . }}-service
          port: 2368
